package test.schedule;

/**
 * The DailyReminder class generates a new ScheduledReminder
 * each day. This will occur at 12:00AM.
 * 
 * A DailyReminder has:
 * name (required)
 * time (optional)
 * description (optional)
 * location (optional)
 * 
 * Getters and setters for the above methods
 * 
 * generate() ScheduledReminder - creates a new ScheduledReminder
 */
public class DailyReminder {
    
}
